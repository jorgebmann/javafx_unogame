package cards.expand;

import cards.Card;

public class WildCard extends Card{
	
	public WildCard(Color color, Number number, Action action, Wild wild) {
		super(color, number, action, wild);
		
	}
}
